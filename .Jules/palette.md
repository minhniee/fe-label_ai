## 2026-04-28 - [Component Consistency]
**Learning:** Consistently using design system components like `Checkbox` and `Label` instead of native elements ensures unified accessibility behaviors and visual styling across the application.
**Action:** Always check `components/ui/` for available Radix-based components before using native HTML inputs or labels.
